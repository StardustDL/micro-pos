server:
  port: 10600
project:
  version: "0.0.1"
spring:
  application:
    name: "products"
  data:
    mongodb:
      database: "micropos"
      host: "localhost"
      port: 10500
  cloud:
    nacos.discovery.server-addr: "http://localhost:10400"

springdoc:
  swagger-ui:
    use-root-path: true