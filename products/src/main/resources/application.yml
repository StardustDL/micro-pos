server:
  port: 9005
project:
  version: "0.1.0"
spring:
  application:
    name: "products"
  jpa:
    hibernate:
      ddl-auto: "update"
  datasource:
    driver-class-name: "com.mysql.cj.jdbc.Driver"
    url: "jdbc:mysql://${MYSQL_HOST:localhost}:33306/products"
springdoc:
  swagger-ui:
    use-root-path: true